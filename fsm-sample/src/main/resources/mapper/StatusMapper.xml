<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.gittab.fsmsample.mapper.StatusMapper">

    <select id="findById" resultType="net.gittab.fsmsample.dto.StatusDTO">
        SELECT *
        FROM fsm_status
        WHERE id = #{id}
    </select>

</mapper>